name: squid-buildx

on:
  schedule:
    - cron: "30 0 * * Sun"
  workflow_dispatch:
  
jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      -
        name: clone https://github.com/heywoodlh/dockerfiles
        uses: actions/checkout@master
        with:
          name: heywoodlh/dockerfiles
          repository: heywoodlh/dockerfiles
          path: ./dockerfiles
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v1
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      -
        name: Login to DockerHub
        uses: docker/login-action@v1 
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      -
        name: Build and push heywoodlh/squid
        uses: docker/build-push-action@v2
        with:
          context: ./dockerfiles/squid
          file: ./dockerfiles/squid/Dockerfile
          platforms: linux/amd64,linux/arm64,linux/armhf
          push: true
          tags: heywoodlh/squid:latest
      - 
        name: Get squid version
        id: version
        run: echo "squid_version=$(docker run -it --rm heywoodlh/squid:latest squid --version | grep 'Squid Cache' | awk '{print $4}')" >> $GITHUB_ENV
        run: echo "::set-output name=version::$()"
      -
        name: Build and push heywoodlh/squid:${{ env.squid_version }}
        uses: docker/build-push-action@v2
        with:
          context: ./dockerfiles/squid
          file: ./dockerfiles/squid/Dockerfile
          platforms: linux/amd64,linux/arm64,linux/armhf
          push: true
          tags: heywoodlh/squid:${{ env.squid_version }}
